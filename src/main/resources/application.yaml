spring:
  application:
    name: "ddd demo"
    description: "This is demo for ddd workshop"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: root
    password: 122333
  h2:
    console:
      enabled: true
      path: /h2

server:
  port: 8000

swagger:
  protocol: http

api:
  version: v1